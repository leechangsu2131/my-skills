interface:
  display_name: "NEIS 성취수준 자동 입력"
  short_description: "성취기준별 단계표를 안전하게 NEIS에 입력"
  default_prompt: "Use $classmanage-evaluate-to-neis to parse the achievement-level table and prepare a safe NEIS dry-run before entering results."
